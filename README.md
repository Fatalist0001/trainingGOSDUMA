# trainingGOSDUMA

ML-эксперимент по прогнозированию результатов выборов в Государственную Думу РФ на уровне регионов.

## TL;DR

- **Что это.** Учимся предсказывать, какую долю голосов получат партии в каждом из 83 регионов России, используя **только прошлые выборы и социально-экономические показатели**. Проверяем модели честно: обучаемся на прошлом, тестируем на будущем (никакого «подсматривания» в будущее).
- **Данные.** 83 региона, парламентские выборы 2003 / 2007 / 2011 / 2016 / 2021. Таргеты — доли трёх партий: **«Единая Россия», КПРФ, ЛДПР**. Вместе они дают ~78% голосов (остальные партии в данных не представлены).
- **Лучшая модель — Transformer** (временная/последовательная нейросеть с механизмом внимания). Её ошибка (MAE) — **6.2 п.п. на 2016** и **6.7 п.п. на 2021**, что лучше и наивного бейзлайна (6.97 / 10.27), и лучших деревьев.
- **Прогноз на 2026 (лучшая модель, взвешивание по числу избирателей):**
  - «Единая Россия» ≈ **64.8%**, КПРФ ≈ **22.9%**, ЛДПР ≈ **12.3%** *(композиция трёх партий, нормированная к 100% — см. пояснение ниже)*.
  - **Важно:** модель предсказывает только эти три партии. Их доля в **прошлых** парламентских выборах (2003–2021) в среднем составляла ~78% — но для 2026 это лишь **предположение**, поскольку итоги 2026 ещё неизвестны. Поэтому вышеприведённые числа — **не** доли от общего числа голосов. Если предположить, что доля прочих партий (вне данных) сохранится на уровне ~22%, то оценочные **реальные доли от всего голосования**: «Единая Россия» ≈ **50.5%**, КПРФ ≈ **17.9%**, ЛДПР ≈ **9.6%**, остальные партии ≈ **22%**.
  - *(Выборы 2026 ещё не прошли — это оценка модели по историческому тренду, а не реальный итог.)*

---

## Что это за проект (простыми словами)

Государственная Дума — нижняя палата парламента РФ, выбирается по партийным спискам. В каждом регионе партия получает какую-то **долю голосов** (в процентах). Мы хотим понять: **насколько точно можно предсказать эту долю заранее**, опираясь на то, что мы знаем про регион из прошлого.

Это не «предсказание победителя» и не политическое исследование — это **воспроизводимая задача машинного обучения на табличных данных**:
- на входе — признаки региона (результаты прошлых выборов, явка, ВРП, зарплата, безработица и т.д.);
- на выходе — доля голосов трёх ключевых партий.

Главный методологический принцип: **временные сплиты**. Мы обучаем модель на истории до некоторого года и проверяем на самом этом годе. Например, обучаемся на 2003–2011 и смотрим, попадём ли в результаты 2016. Это имитирует реальное прогнозирование, где будущее неизвестно. Никакого случайного перемешивания регионов — только «прошлое → будущее».

---

## Данные и эксперименты

- **Источник:** [datasetGOSDUMA](https://github.com/Fatalist0001/datasetGOSDUMA) → `data/processed/master_region_election.parquet` (уровень регионов, 83 региона, годы 2000–2024).
- **Целевые партии (доли голосов, %):** `UR_share` («Единая Россия»), `KPRF_share` (КПРФ), `LDPR_share` (ЛДПР). Других партий в датасете нет. В парламентские годы эти три покрывают **~78%** голосов (медиана 79.5%); оставшиеся ~22% — прочие партии вне данных. Поэтому **нормализация предсказаний к 100% не применяется** — каждая доля прогнозируется независимо.
- **Признаки:** электоральные (результаты прошлых выборов, явка) + Росстат (ВРП на душу, зарплата, безработица, бедность, урбанизация и т.д., с лагами `_lag1`).
- **Важный нюанс данных.** Год 2024 помечен как `type="pres"` (президентские), а не парламентские. Парламентские (ГД) годы в данных: **2003, 2007, 2011, 2016, 2021**. В президентские годы доли парламентских партий не записаны (равны 0), поэтому для обучения и теста берутся только парламентские годы.

**Эксперименты (history-backtest):**
- **A:** обучение 2003–2011 → тест **2016**.
- **B:** обучение 2003–2016 → тест **2021**.

Это единственные эксперименты с эталонными метками. Эксперименты **C** и **D** (тест 2026) — это финальный прогноз будущего, у них нет правильных ответов; считаются скриптом `predict_2026.py`.

**Метрика — MAE** (Mean Absolute Error, средняя абсолютная ошибка), измеряется в процентных пунктах (п.п.):

$$\text{MAE} = \frac{1}{n}\sum_{i=1}^{n} \left| y_i^{\text{true}} - y_i^{\text{pred}} \right|$$

где $y$ — доля партии в регионе. MAE считается отдельно по каждой партии, а в сводных таблицах усредняется по трём партиям. **Чем меньше MAE — тем точнее прогноз.**

---

## Методология (почему результатам можно верить)

1. **Только временные сплиты** — прошлое обучает, будущее тестирует. Никакого `random_state`/перемешивания.
2. **Нет утечки данных (leakage)** — масштабирование и заполнение пропусков подгоняются (fit) только на обучающих годах и применяются к тесту. Лаговые признаки уже подготовлены в датасете и не содержат будущего.
3. **Динамические таргеты** — список партий берётся из данных, не зашивается в код.
4. **Три группы признаков** для абляции: `ELECTORAL_ONLY` (только прошлые выборы), `ROSSTAT_ONLY` (только социо-экономика), `ALL_FEATURES` (всё вместе).
5. **Гиперпараметры подбираются только на train**; нейросети усредняются по 5 seeds (42, 123, 456, 789, 2026) для устойчивости.

---

## Модели и результаты (P0–P3)

Ниже — MAE по всем моделям на экспериментах A (2016) и B (2021). Все числа — из `results/benchmark_all_20260819.csv`.

### Что это за модели (простым языком)

**Бейзлайны** — это не «обучение», а простые ориентиры: как бы предсказал выборы человек, который почти ничего не считает.
- *NaivePreviousElection* — «возьми прошлый результат»: если в прошлый раз у партии было 50%, пусть так и останется. Самое наивное, но очень сильное: голосование в регионах инертно.
- *HistoricalMean* — среднее по всем прошлым выборам.
- *WeightedHistoricalMean* — то же, но свежие выборы весят больше (как будто «недавнее важнее давнего»).
- Зачем они нужны: если сложная модель не обгоняет «просто возьми прошлый результат», значит она ничего не добавила.

**Линейные модели** (`LinearRegression`, `Ridge`, `ElasticNet`) — это уравнение вида «результат = вес₁·признак₁ + вес₂·признак₂ + … + константа». Модель сама подбирает веса так, чтобы уравнение точнее объясняло прошлые годы. `Ridge` и `ElasticNet` добавляют «штраф» за большие веса, чтобы модель не цеплялась за шум, а `ElasticNet` дополнительно умеет вообще занулять неважные признаки. Плюсы: быстрые, простые, понятные. Минусы: не умеют улавливать нелинейные эффекты («больше зарплата — резко больше голосов» они так не смоделируют).

**Деревья и их ансамбли** (`RandomForest`, `HistGradientBoosting`, `XGBoost`, `CatBoost`). Одно *дерево решений* — это цепочка вопросов-развилок: «зарплата в регионе выше среднего? да → спроси про явку; нет → …». По ответам оно делит регионы на похожие группы и выдаёт среднее по группе. Само дерево — модель слабая, поэтому собирают **ансамбли**: `RandomForest` — это много независимых деревьев на разных подвыборках, которые «голосуют»; `XGBoost`, `CatBoost`, `HistGradientBoosting` — бустинг: каждое следующее дерево пытается исправить ошибки предыдущих. Это «рабочие лошадки» табличных данных: хорошо ловят нелинейности и взаимодействия, но на маленьких данных склонны переобучаться.

**KNN (ближайшие соседи)** — ничего не «учит», а при прогнозе ищет k самых похожих регионов из прошлого и усредняет их результат. Проверяет простую гипотезу: «похожие регионы голосуют похоже». Как правило, слабее деревьев, но полезен как контроль.

**Нейросети-MLP** (`MLPSklearn`, `MLPTorch`) — многослойная сеть «нейронов» со связями; в принципе может воспроизвести почти любую закономерность. На больших данных — сила, но здесь обучается на ~83 регионах, поэтому переобучается (запоминает примеры вместо закономерностей). `MLPSklearn` — простая реализация из scikit-learn; `MLPTorch` — гибкая на PyTorch, но требует тщательной настройки, которой в этом сетапе нет.

**Временные модели** (`GRU`, `LSTM`, `Transformer`) — единственные, кто смотрит на историю региона как на **последовательность** (2003 → 2007 → 2011 → …), а не как на «мешок признаков». Они учатся, какие прошлые годы важнее и как результат меняется со временем.
- *GRU* и *LSTM* — рекуррентные сети с «памятью»: проходят историю шаг за шагом, запоминая важное и забывая лишнее.
- *Transformer* — использует механизм **внимания (attention)**: сразу сравнивает все прошлые годы между собой и сам определяет, какие выборы наиболее информативны для прогноза. Это единственная модель, которая обошла наивный бейзлайн на обоих тестовых годах.

### Бейзлайны (наивные ориентиры)

Не обучаются на признаках, а просто используют прошлые результаты. Нужны, чтобы понять, даёт ли сложная модель вообще преимущество.

| Модель | A (2016) | B (2021) |
|--------|---------:|---------:|
| NaivePreviousElection (копия прошлого) | **6.97** | 10.27 |
| WeightedHistoricalMean (взвеш. прошлое) | 7.04 | 10.46 |
| HistoricalMean (среднее по прошлому) | 7.93 | 10.11 |

### Линейные модели

| Модель / группа | A (2016) | B (2021) |
|-----------------|---------:|---------:|
| LinearRegression (ALL) | 11.91 | 9.93 |
| LinearRegression (ELECTORAL) | 12.66 | 8.83 |
| LinearRegression (ROSSTAT) | 10.12 | 10.76 |
| Ridge (ALL) | 11.26 | 9.93 |
| Ridge (ELECTORAL) | 12.41 | 8.86 |
| Ridge (ROSSTAT) | 9.75 | 10.51 |
| ElasticNet (ALL) | 10.86 | 9.50 |
| ElasticNet (ELECTORAL) | 12.48 | 8.85 |
| **ElasticNet (ROSSTAT)** | **9.33** | 10.77 |

### Деревья (ансамбли решений)

| Модель / группа | A (2016) | B (2021) |
|-----------------|---------:|---------:|
| RandomForest (ALL) | 7.91 | 10.38 |
| RandomForest (ELECTORAL) | 9.08 | 10.42 |
| RandomForest (ROSSTAT) | 7.98 | 11.21 |
| HistGradientBoosting (ALL) | 8.44 | 11.43 |
| HistGradientBoosting (ELECTORAL) | 8.70 | 11.47 |
| HistGradientBoosting (ROSSTAT) | 8.11 | 11.34 |
| XGBoost (ALL) | 7.30 | 10.46 |
| XGBoost (ELECTORAL) | 8.96 | 10.51 |
| XGBoost (ROSSTAT) | 7.80 | 10.46 |
| **CatBoost (ELECTORAL)** | 8.58 | **9.92** |
| CatBoost (ALL) | 7.76 | 10.12 |
| CatBoost (ROSSTAT) | 7.76 | 10.54 |

### KNN (ближайшие соседи)

| Модель / группа | A (2016) | B (2021) |
|-----------------|---------:|---------:|
| **KNN (ALL)** | 7.76 | 10.82 |
| KNN (ELECTORAL) | 9.90 | 10.77 |
| KNN (ROSSTAT) | 8.14 | 11.45 |

### Нейросети (MLP — sklearn и PyTorch)

| Модель / группа | A (2016) | B (2021) |
|-----------------|---------:|---------:|
| **MLPSklearn (ALL)** | 8.42 | 12.15 |
| MLPSklearn (ELECTORAL) | 7.55 | 10.39 |
| MLPSklearn (ROSSTAT) | 9.77 | 11.99 |
| **MLPTorch (ALL)** | 12.81 | 12.42 |
| MLPTorch (ELECTORAL) | 8.66 | 11.14 |
| MLPTorch (ROSSTAT) | 12.00 | 12.40 |

### Временные модели (GRU, LSTM, Transformer) — P2/P3

Воспринимают историю региона как **последовательность** (2003 → 2007 → 2011 → …) и учатся, какие прошлые годы важнее. Реализованы на PyTorch; пропуски заполняются медианой, масштабирование — только на train (без утечки).

| Модель / группа | A (2016) | B (2021) |
|-----------------|---------:|---------:|
| **Transformer (ALL)** | **6.20** | **6.70** |
| Transformer (ELECTORAL) | 10.86 | 7.93 |
| Transformer (ROSSTAT) | 7.03 | 8.99 |
| **GRU (ALL)** | 6.88 | 7.60 |
| GRU (ELECTORAL) | 11.66 | 6.52 |
| GRU (ROSSTAT) | 10.61 | 10.08 |
| LSTM (ALL) | 11.26 | 8.10 |
| LSTM (ELECTORAL) | 10.68 | 7.03 |
| **LSTM (ROSSTAT)** | 14.21 | 9.68 |

### Итоговая сводка (MAE, меньше = лучше)

| Модель | A (2016) | B (2021) |
|--------|---------:|---------:|
| **Transformer (ALL)** | **6.20** | **6.70** |
| GRU (ALL) | 6.88 | 7.60 |
| NaivePreviousElection | 6.97 | 10.27 |
| WeightedHistoricalMean | 7.04 | 10.46 |
| XGBoost (ALL) | 7.30 | 10.46 |
| GRU (ELECTORAL) | 11.66 | 6.52 |
| CatBoost (ALL) | 7.76 | 10.12 |
| RandomForest (ALL) | 7.91 | 10.38 |
| KNN (ALL) | 7.76 | 10.82 |
| MLPSklearn (ALL) | 8.42 | 12.15 |
| MLPTorch (ALL) | 12.81 | 12.42 |
| HistoricalMean | 7.93 | 10.11 |
| ElasticNet (ROSSTAT) | 9.33 | 10.77 |
| CatBoost (ELECTORAL) | 8.58 | 9.92 |
| LSTM (ROSSTAT) | 14.21 | 9.68 |

---

## Почему результаты именно такие (ключевые вопросы исследования)

Здесь разбираем, что показал эксперимент, и **почему** — без ссылок на внешние документы.

**Вопрос 1. Бьют ли ML-модели наивный бейзлайн?**
Результаты выборов в регионах **очень стабильны из года в год** (высокая автокорреляция): доля партии сегодня почти равна её доле в прошлый раз. Поэтому «просто возьми прошлый результат» (NaivePreviousElection, 6.97/10.27) — уже сильный прогноз, и на 2016 ни одна «плоская» модель его не обошла. Преимущество сложных моделей проявляется на **более свежем и необычном** 2021 году (Transformer 6.70 против 10.27). Вывод: ML даёт выигрыш, но он существенен прежде всего на «трудных» годах.

**Вопрос 2. Добавляют ли социально-экономические показатели (Росстат) предсказательную силу?**
**Да, но преимущественно при короткой истории.** На эксперименте A (всего 3 прошлых выбора) социо-экономика заметно помогает: линейные модели на ROSSTAT лучше, чем на одних выборах (ElasticNet 9.33 против 10.86); деревья на ROSSTAT точнее, чем на ELECTORAL (XGBoost 7.80 vs 8.96). На эксперименте B (истории больше) Росстат нейтрален или **мешает** деревьям (CatBoost: ELECTORAL 9.92 < ALL 10.12 < ROSSTAT 10.54).
*Почему:* при малом числе прошлых выборов прошлый результат сам по себе малоинформативен, и соц-экономика даёт дополнительный сигнал; при длинной истории прошлый результат уже говорит сам за себя, а Росстат шумит.

**Вопрос 3. Помогает ли более длинная история прошлых выборов?**
**Да — для временных моделей.** Transformer на A улучшается по мере удлинения истории (depth 2 → 3: 7.03 → 6.20), а на B лучший результат — при полной истории (depth 4, 6.70). У «плоских» моделей отдача шумная и убывающая. Наивный бейзлайн от глубины истории не зависит (берёт только последний год).
*Почему:* извлечь пользу из длинной истории можно только если модель **учитывает порядок во времени** (последовательная архитектура), а не просто усредняет признаки.

**Вопрос 4. Есть ли преимущество у нелинейных моделей (деревья, KNN) над линейными?**
**В среднем — да, но неуниверсально.** Средний MAE по A/B (ALL): линейные **10.57**, деревья+KNN **9.24**, временные **7.79**. На A (2016) преимущество нелинейности огромно (7.83 против 11.34 у линейных: линейные «разваливаются» на КПРФ), но на B (2021) линейные **выигрывают** (9.79 против 10.64 у деревьев; у ElasticNet на B даже 9.50).
*Почему:* деревья умеют моделировать нелинейности и взаимодействия признаков — это решает при короткой истории (A). Но на «аномальном» 2021 деревья переобучаются под исторический паттерн, тогда как гладкая линейная зависимость даёт более консервативный, устойчивый прогноз. Итог: нелинейность даёт выигрыш, но не заменяет учёт времени (лучшая модель — всё равно temporal, а не дерево).

**Вопрос 5. Помогают ли нейросети-MLP (плоские)?**
**Практически нет.** `MLPSklearn` конкурентоспособен местами (на A ELECTORAL 7.55), но `MLPTorch` заметно слабее всех (на малом табличном датасете ~83 региона сеть переобучается). На небольших табличных данных нейросети редко обходят деревья без тщательной настройки.

**Вопрос 6. Помогают ли временные (последовательные) модели GRU/LSTM/Transformer?**
**Да — это главный результат проекта.** Они обходят и плоские нейросети, и деревья, и наивный бейзлайн на обоих годах. **Transformer (ALL) — лучшая модель вообще** (6.20/6.70); GRU близко второй (6.88/7.60). LSTM — самая нестабильная (на A ROSSTAT 14.21: взрыв градиента/переобучение).
*Почему:* для электоральных рядов важен именно учёт **порядка во времени**, а не просто глубина сети.

**Вопрос 7. Какие партии предсказываются лучше?**
**ЕР — самая сложная партия, ЛДПР — самая простая.** Средний MAE за A/B (п.п.):

| Модель | ЕР | КПРФ | ЛДПР |
|--------|-----|------|------|
| Transformer (ALL) | **7.95** | 5.70 | 5.70 |
| GRU (ALL) | 9.12 | 7.15 | **5.45** |
| CatBoost (ALL) | 16.48 | **5.04** | 5.30 |
| XGBoost (ALL) | 16.04 | 5.11 | 5.49 |
| NaivePreviousElection | 12.19 | 7.21 | 6.45 |

У лучших временных моделей разрыв между партиями мал (ЕР 8–9, КПРФ/ЛДПР ~5.5–7); у плоских деревьев ЕР «проваливается» (16 п.п.) — её ошибка съедает почти весь выигрыш.
*Почему:* у ЕР самый большой разброс по регионам и высокая волатильность от года к году; ЛДПР держится в компактном диапазоне (~10–15%), и её уровень проще угадать. Частично это масштабный эффект: MAE растёт вместе с уровнем поддержки партии. Данные: `results/q7_party_mae.csv`.

**Вопрос 8. В каких регионах модели систематически ошибаются?**
**Хуже всего — крупные города и северные/удалённые регионы; лучше всего — Северный Кавказ и стабильные республики.**
- Худшие (средний MAE по 7 моделям): на A (2016) — Москва, Кировская обл., Санкт-Петербург, Тамбовская, Вологодская, Амурская; на B (2021) — Якутия, Москва, Марий Эл, Коми, Ненецкий АО, Калмыкия.
- Лучшие: Кабардино-Балкария, Карачаево-Черкесия, Тыва, Северная Осетия, Мордовия (A); Кабардино-Балкария, Башкортостан, Ингушетия, КЧР, Тыва (B).
- В худших регионах систематический сдвиг **положительный** (модели завышают поддержку): Москва +11–12 п.п., Ярославская обл. +12.4 п.п. на B.
*Почему:* модели хорошо выучивают «ядро» электоральной стабильности (в республиках Северного Кавказа уровень ЕР высокий и почти не меняется — ошибка минимальна), но плохо ловят **структурные сдвиги** 2021 в регионах с волатильным голосованием. Данные: `results/q8_regional_errors.csv`.

**Вопрос 9. Насколько стабилен результат по seed-ам?**
**Умеренно стабилен, и стабильность коррелирует с качеством.** Усреднение по 5 seeds [42,123,456,789,2026], std MAE по партиям:

| Модель | A (2016) mean±std | B (2021) mean±std | max разброс min–max |
|--------|-------------------|-------------------|---------------------|
| **Transformer** | 6.20±0.43 | 6.70±0.54 | 1.49 |
| **GRU** | 6.88±0.75 | 7.60±0.26 | 1.92 |
| LSTM | 11.26±1.16 | 8.10±0.57 | 3.01 |
| MLPSklearn | 8.42±0.73 | 12.15±1.51 | **4.69** |
| MLPTorch | 12.81±1.39 | 12.42±0.81 | 3.85 |

Самые стабильные — **Transformer и GRU** (лучшие и при этом устойчивые). Наименее стабильные — LSTM и MLPTorch (переобучаются на малом датасете и чувствительны к инициализации). Для лучших моделей разница между худшим и лучшим seed — ~1.5 п.п., что меньше их отрыва от остальных моделей. Данные: `results/q9_seed_stability.csv`.

**Вопрос 10. Помогают ли ансамбли (объединение моделей)?**
**Нет, в этом сетапе.** WeightedEnsemble (взвешивание по обратной ошибке) и StackingEnsemble (мета-модель поверх баз) **не превзошли** лучшую одиночную модель (Transformer), и даже XGBoost. Их MAE — 7.91/10.83 и 8.57/10.94 против 6.20/6.70 у Transformer.
*Почему:* базовые модели неоднородны по «слабой» партии (линейные хуже всего на КПРФ, деревья — на ЕР, см. Вопрос 7), а усреднение усредняет и их провалы, не компенсируя их; лучшая одиночная модель (Transformer) уже захватывает весь сигнал.

---

## Детальнее: абляция (фич-группы и глубина истории)

- **Фич-группы (вопрос 2).** ROSSTAT реально добавляет силу при короткой истории (A): деревья на ROSSTAT_ONLY точнее, чем на ELECTORAL_ONLY (XGBoost 7.80 vs 8.96, CatBoost 7.76 vs 8.58). На длинной истории (B) — нейтрально/мешает (CatBoost ELECTORAL 9.92 < ALL 10.12 < ROSSTAT 10.54). Для временных моделей лучший вариант — ALL; на A ROSSTAT помогает Transformer (7.03 < 10.86 на ELECTORAL).
- **Глубина истории (вопрос 3).** См. разбор выше: длинная история улучшает Transformer и GRU, у плоских моделей — шумная отдача.

Результаты абляции — в `results/ablation_feature.csv` и `results/ablation_history.csv` (генерируются `make ablation`).

---

## Детальнее: ансамбли (вопрос 10)

`WeightedEnsemble` взвешивает базовые модели по обратной OOF-ошибке **отдельно для каждой партии**; `StackingEnsemble` обучает линейную мета-модель на OOF-прогнозах баз. Базы: XGBoost, CatBoost, RandomForest, HistGradientBoosting, MLPSklearn, LinearRegression (бейзлайны исключены — они игнорируют признаки). Вывод: ансамбли не дали преимущества над лучшей одиночной моделью (см. выше, «Вопрос 10»). Результаты — в `results/ensemble_*.json`.

---

## Финальный прогноз на 2026 (эксперименты C/D)

Эксперименты **C** и **D** обучаются на всей истории (2003–2021) и прогнозируют 2026 — эталонных меток нет, это чистый прогноз (`predict_2026.py`, функция `forecast_temporal`). Лучшая модель (**Transformer, ALL**) дообучается на всех данных и предсказывает доли по 83 регионам, усреднение по 5 seeds.

**Федеральный прогноз (взвешивание по числу избирателей):**
- «Единая Россия» ≈ **64.8%**
- КПРФ ≈ **22.9%**
- ЛДПР ≈ **12.3%**

⚠️ **Это композиция трёх партий, нормированная к 100%.** Модель предсказывает только ЕР/КПРФ/ЛДПР, которые в парламентских годах покрывают ~78% голосов (остальные ~22% — партии вне датасета). Поэтому вышеприведённые числа **не являются долями от общего числа голосов**. Если предположить, что доля прочих партий сохранится на уровне ~22%, то оценочные **реальные доли от всего голосования** составят:
- «Единая Россия» ≈ **50.5%**
- КПРФ ≈ **17.9%**
- ЛДПР ≈ **9.6%**
- прочие партии ≈ **22%**

Это экстраполяция исторического тренда моделью; для валидации нужны реальные итоги 2026 года.

---

## Визуализация

Ключевые графики (генерируются `make report` → `scripts/visualization/generate_report.py`, сохраняются в `reports/figures/`):

![Сравнение моделей (MAE, ALL_FEATURES, эксп. A и B)](reports/figures/model_comparison.png)

*Рис. 1. MAE по всем моделям на группе ALL_FEATURES. Transformer (ALL) — лучший на обоих экспериментах.*

![Абляция по фич-группам (вопрос 2, эксп. A)](reports/figures/feature_ablation.png)

*Рис. 2. MAE в зависимости от группы признаков. ROSSTAT помогает на короткой истории (A), но нейтрален/мешает на длинной (B).*

![Абляция по глубине истории (вопрос 3, A и B)](reports/figures/history_depth.png)

*Рис. 3. MAE временных моделей в зависимости от глубины истории. Длинная история улучшает Transformer и GRU; LSTM нестабилен.*

Остальные графики из плана (actual vs predicted, ошибка по годам, важность признаков, SHAP, карта регионов) пока не реализованы.

---

## Структура проекта

```
trainingGOSDUMA/
├── config/        # paths.yaml, experiments.yaml, features.yaml, models.yaml
├── src/
│   ├── data/      # loader, splits, preprocessing, features
│   ├── models/    # baselines, linear, trees, knn, neural, temporal, ensemble, registry
│   ├── evaluation/# metrics, backtest, ablation, analysis
│   ├── tracking/  # логирование экспериментов (JSON)
│   ├── visualization/  # generate_report.py (графики)
│   └── utils/     # воспроизводимость, I/O
├── scripts/       # точки входа (run_baselines, run_linear, run_trees, run_knn, run_neural, run_temporal, benchmark, ablation, predict_2026)
├── tests/         # pytest: splits, leakage, ablation, ensemble (30 тестов)
├── results/       # benchmark_all_*.csv, ablation_*.csv, ensemble_*.json, forecast_*_federal.csv, predictions/ (parquet)
├── reports/       # FINAL_MODEL_REPORT.md, figures/ (графики)
├── logs/          # логи запусков
├── PROGRESS.md    # отслеживание выполнения PLAN.md
├── AGENTS.md      # архитектура и правила
└── README.md      # этот файл
```

---

## Статус и следующие шаги

- ✅ **P0**: baselines + Linear + Trees + KNN на A/B по 3 фич-группам. Сводная таблица — `results/benchmark_all_*.csv`. Тесты (30) и `ruff`-lint проходят.
- ✅ **P1**: нейросети (`neural.py`, `run_neural.py`) — ответ на вопрос 5.
- ✅ **P2/P3**: временные модели GRU/LSTM/Transformer (`temporal.py`, `run_temporal.py`) — ответ на вопрос 6; **Transformer (ALL) — лучшая модель** (A 6.20, B 6.70).
- ✅ **Абляция**: `make ablation` → `results/ablation_feature.csv` (вопрос 2) и `results/ablation_history.csv` (вопрос 3).
- ✅ **Ансамбли** (вопрос 10): `WeightedEnsemble`/`StackingEnsemble` (`ensemble.py`) — не превосходят лучшую одиночную модель.
- ✅ **Прогноз 2026**: эксперименты C/D через `predict_2026.py` (Transformer, ALL). Федеральный прогноз: ЕР 64.8 / КПРФ 22.9 / ЛДПР 12.3.
- ✅ **Отчёт и визуализация**: `reports/FINAL_MODEL_REPORT.md` и графики (`reports/figures/`, `make report`).
- ⏳ **Возможные доработки**: пересчёт долей в мандаты (seat allocation), bootstrap-CI для устойчивости MAE, SHAP-анализ важности признаков, подключение президентского 2024 года в дообучение перед финальным прогнозом.

---

## Установка и запуск

```bash
uv sync --dev          # установить зависимости (см. pyproject.toml)
uv run pytest tests/    # проверка сплитов и отсутствия leakage
make benchmark         # полный P0 backtest + сводная таблица
make ablation          # абляция по фич-группам и глубине истории
make report            # сгенерировать графики в reports/figures/
python scripts/predict_2026.py   # прогноз 2026 (Transformer)
```

Основные `make`-цели: `test`, `lint`, `baselines`, `linear`, `trees`, `knn`, `neural`, `temporal`, `benchmark`, `ablation`, `report`, `clean`.

Переменные окружения: `DATASET_ROOT` (путь к датасету [datasetGOSDUMA](https://github.com/Fatalist0001/datasetGOSDUMA), по умолчанию `/home/alexsander/datasetGOSDUMA`).
